Problems:
Error parsing in Truffle.
Solution:
Update versions of Node, Truffle and npm

Client is Ganache so testrpc was not found.
Solution:
npm install -g ganache-cli
and
ganache-cli
which basically is same as testrpc.
